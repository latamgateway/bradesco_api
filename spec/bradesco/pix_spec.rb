# frozen_string_literal: true

RSpec.describe Bradesco::Pix do
  it "has a version number" do
    expect(Bradesco::Pix::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
